import React from "react";

const EmailInput = () => {
  return (
    <input
      className="email-input"
      type="email"
      placeholder="Enter email"
      required
    ></input>
  );
};

export default EmailInput;
